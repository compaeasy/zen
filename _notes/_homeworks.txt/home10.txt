1. Реализовать компонент Form

2. Почитать про методы массивов 

3. Выполнить несколько задач по методам массивов: 
  1. обьявить переменную, присвоить ей массив, с помощью метода массивов добавить элемент начала массива и вывести результат (переменная массив) в браузере 
  2. с помощью метода массивов добавить элемент в конец массива и вывести результат в браузере.
  3. с помощью метода массива удалить элемент в начале массива .
  4. с помощью метода массива удалить элемент в конце массива.


  1. объявить переменную и присвоить ей массив числовых элементов.
  2. с помощью метода массивов вычислить сумму элементов массива и вывести результат. - (Метод .reduce())
  3. с помощью метода массивов вычислить произведение элементов массива и вывести результат. - (Метод .reduce())

  1. объявить переменную и присвоить ей массив числовых элементов. Длина массива 10 элементов.  
  2. с помощью метода массивов вернуть новый массив элементов, которые являются четными.  - (Метод .filter())

4. Убедиться в том, что стилистика кода в компонентах одинаковая, проставленные JSDoc комментарии, реализованы все необходимые компоненты, в браузере выполняется  рендеринг всех элементов интерфейса, у элементов интерфейса проставлены классы по методологии БЭМ.

5. Установить Node.js 

6. Установить SASS 
