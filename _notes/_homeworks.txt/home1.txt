/* Домашнее задание 
1. Логику написанную в учебном проекте вдумчиво прочитать, осознать и воспроизвести в собственном проекте
2. Проделать упражнение на предмет итерации массива: 
* создать массив с элементами одного типа данных
* проитерировать в цикле for...of созданный массив и вывести во время итерации каждый элемент в консоль браузера
* проитерировать в цикле созданный массив с помощью метода итерации массивов (.forEach()) и вывести во время итерации каждый элемент в консоль браузера
* проитерировать в цикле созданный массив с помощью метода итерации массивов (.map()), чтобы вернуть новый массив преобразованных элементов и присвоить этот новый возвращенный массив новой переменной, затем эту переменную вывести в консоль браузера
P.s Не забываем про callback функции и метод .join(), который преобразует массив к строке.
3. Компоненты Section, Title, Text и Image должны соответствовать по функционалу к шестому занятию
4. Почитать про озвученные методы массивов
5. Прочитать про методы строк:
  .toUpperCase()
  .toLowerCase()
  .at(-1)
  .trim()
  .substring()
  .startsWith()
  .endsWith()
  .slice()
  .replace() .replaceAll()
  .includes()
  .search()
  .split()
*/