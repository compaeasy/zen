1. Реализовать логику в компоненте Title, Text, Image таким образом, чтобы осуществлялась проверка на предмет получения извне родительского класса и если он передается, то в html шаблон, (который возвращает каждый из этих 3 компонентов) должен подставляться составной класс: 
block__element если родительский класс не передается при вызове этих компонентов, то должен подставляться каждый в html шаблон, (который возвращает каждый из этих 3 компонентов) класс:element