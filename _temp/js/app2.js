/*
   return data.map((element) => {
    switch (element.type) {
      case 'header':
        return Header(element)
      case 'section':
        return Section(element)
      case 'cashback':
        return CashBack(element)
      case 'clients':
        return Clients(element)
      case 'footer':
        return Footer(element)
    }
  }).join('')
 */